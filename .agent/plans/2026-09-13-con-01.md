# ExecPlan: CON-01

<!-- status: DONE; completed_action_id: CON-01:INDEPENDENT-REVIEW -->

## Outcome

Congelar os contratos das primeiras jornadas antes de liberar consumidores paralelos. O contrato deve tornar versao, sucesso, erro, estado parcial, logout e saldo financeiro observaveis sem prometer revogacao server-side nao observada nem inventar regra de estorno.

## Scope

- Base: HEAD `1c22c5dc79c52151f4c7c94c4402dfdc86812cbc` mais o overlay documental de ARC-01.
- Exclusivo: `contracts`.
- Arquivos reservados: `packages/contracts/src/index.ts`, `packages/contracts/src/api-catalog.ts` e `docs/adr/030-contracts-session-finance-compatibility.md`.
- Fora do escopo: API, web, banco, migrations, testes, lockfile, provider externo e release.

## Completed Work

- Added versioned success/error/partial envelopes, bounded pending operations and explicit error code coverage.
- Added command receipt terminal invariants, receipt references, session lifecycle and logout server-observation invariants.
- Added charge/payment status schemas and financial balance arithmetic with explicit `UNKNOWN`/`REQUIRES_POLICY` states.
- Added `FIRST_JOURNEY_CONTRACT_MANIFEST`, schema registry and executable synthetic examples in the contracts package.
- Hardened `upcastApiValue()` so unsupported target schema versions fail closed.
- Bounded error details by key/node/depth/string limits and made `failure()` replace invalid diagnostic payloads with a safe marker.
- Guarded synchronous and asynchronous schema entry points against hostile accessors, proxies, sparse arrays and non-JSON data graphs.
- Round-1 independent review scored 8.5/10 and was not accepted; its unbounded-details finding was corrected in the current overlay.
- Final independent review passed at 9.5/10 with no blocking findings in the reserved files.
- Local checks passed: `npm run test:contract` (5/5), `npm run typecheck`, `npm test` (329 pass, 1 skip), `npm run lint` (169 files), direct hostile sync/async matrix, catalog/upcaster probe and diff checks.

## Concrete Steps

1. ~~**CON-01:INDEPENDENT-REVIEW** — Submeter o diff, os schemas e a evidencia local a critica independente read-only.~~
2. ~~Validar os exemplos exportados e os estados negativos sem alterar consumidores fora da reserva.~~
3. ~~Executar `npm run test:contract`, `npm run typecheck` e `git diff --check`.~~
4. ~~Registrar o veredito fresco e fechar a tarefa somente se o score atingir 9/10 sem achado bloqueante.~~

## Acceptance

- Versao e exemplos de sucesso, erro e estado parcial registrados e parseaveis.
- Logout distingue estado local de revogacao observada pelo servidor.
- Saldo pendente e estados financeiros sao tipados; estorno nao resolvido permanece explicito.

## Recovery

Se a verificacao falhar, preservar o diff, registrar o diagnostico no ledger e corrigir somente os dois arquivos reservados. Nenhum efeito externo foi acionado.

## Closure

- Status: `DONE` no escopo local reservado.
- Gate: `.agent/gates/con-01.json` com `PASS`, score `9.5/10`.
- Independent review: `.gauntlet/critique-con-01-final.md`.
- Verification: `.agent/verification.jsonl#VER-CVG-CON01-003`.
- Global verdict remains `AAA_NOT_PROVEN`; the next planned action is `CON-02:IMPLEMENT`.
