route:
  receiver: cvg-null
  group_by: [alertname, service, environment]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: cvg-null
    # Staging-safe default. Replace with an approved receiver and secret
    # reference before exercising alert delivery.
